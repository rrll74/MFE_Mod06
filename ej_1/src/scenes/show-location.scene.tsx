import * as React from 'react';
import { AppLayout } from 'layouts';
import { ShowLocationContainer } from 'pods/location';

export const ShowLocationScene = () => (
  <AppLayout>
    <ShowLocationContainer />
  </AppLayout>
);
