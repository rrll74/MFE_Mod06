import * as React from 'react';
import { AppLayout } from 'layouts';
import { ShowEpisodeContainer } from 'pods/episode';

export const ShowEpisodeScene = () => (
  <AppLayout>
    <ShowEpisodeContainer />
  </AppLayout>
);
