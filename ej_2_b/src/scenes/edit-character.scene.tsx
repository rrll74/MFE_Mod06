import * as React from 'react';
import { AppLayout } from 'layouts';
import { EditCharacterContainer } from 'pods/character';

export const EditCharacterScene = () => (
  <AppLayout>
    <EditCharacterContainer />
  </AppLayout>
);
