import * as React from 'react';
import { AppLayout } from 'layouts';
import { ShowCharacterContainer } from 'pods/character';

export const ShowCharacterScene = () => (
  <AppLayout>
    <ShowCharacterContainer />
  </AppLayout>
);
